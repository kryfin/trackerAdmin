#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../express-admin/bin/admin" "$@"
  ret=$?
else 
  node  "$basedir/../express-admin/bin/admin" "$@"
  ret=$?
fi
exit $ret
